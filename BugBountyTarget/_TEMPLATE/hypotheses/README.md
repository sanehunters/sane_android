# Hypotheses

One file per untested idea. Each states the hypothesis, why it is plausible, and **the exact experiment
that would confirm or refute it**. Anything speculative is marked `HYPOTHESIS` until that experiment runs.

A hypothesis that survives becomes a finding. One that fails moves to the ruled-out register with the
refutation — it does not get deleted.
